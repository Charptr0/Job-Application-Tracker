export interface IApplicationWarningMessages {
    companyName: {
        visible: boolean,
        message: string,
    },

    positionName: {
        visible: boolean,
        message: string,
    },

    location: {
        visible: boolean,
        message: string,
    },

    link: {
        visible: boolean,
        message: string,
    },

    jobType: {
        visible: boolean,
        message: string,
    },

    status: {
        visible: boolean,
        message: string,
    }
}